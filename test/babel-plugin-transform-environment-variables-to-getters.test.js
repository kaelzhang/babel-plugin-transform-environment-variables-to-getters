const test = require('ava')
const log = require('util').debuglog('babel-plugin-transform-environment-variables-to-getters')
const babel_plugin_transform_environment_variables_to_getters = require('../src')

test('description', t => {
  t.is(true, true)
})
